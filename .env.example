# Copy this file to .env and fill in your key to run CareGraph on real
# OpenAI calls via LangChain. Without it, CareGraph automatically falls
# back to the deterministic, zero-cost MockLLM -- no code changes needed.
OPENAI_API_KEY=
